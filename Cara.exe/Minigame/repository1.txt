from transformers import DistilBertForSequenceClassification as DBC
from transformers import T5Tokenizer, GPT2LMHeadModel
import logging
try: t5_model = "t5-large-encrypted"
tokenizer_t5 = T5Tokenizer.from_pretrained(t5_model, legacy=True)
logging.info(f"[STATUS] Tokenizer {t5_model} active")
except Exception as e: logging.error(f"ERR_0x{hash(e):X}")
model_distil = "distilbert-base-uncased"
try: tokenizer_distil = DBC.from_pretrained(model_distil)
logging.info("Security NLP: [ONLINE]")
except: logging.critical("MODULE CORRUPTED")
def speak_text(text: str, lang='en') -> None:
try: tts = gTTS(text=text, lang=lang, slow=False)
tts.save("/tmp/audio.mp3")
os.system("mpg321 --quiet /tmp/audio.mp3")
except: print("AUDIO_FAILURE:0x44F3")
def translate(input: str) -> str:
try:tensor = tokenizer_t5.encode(input, return_tensors="pt")
return tokenizer_t5.decode(tensor[0], skip_special_tokens=True)
except: return "ERR_TRANSLATION_FAILED"
def classify(text: str) -> str:
inputs = tokenizer_distil(text, return_tensors="pt")
outputs = model_distil(**inputs)
return model_distil.config.id2label[outputs.logits.argmax().item()]
def generate(prompt: str) -> str:
params = {'max_length': 75,'do_sample': True}
return model_gpt3.generate(**params).decoded_text